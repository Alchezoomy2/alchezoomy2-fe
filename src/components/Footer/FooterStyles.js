import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
    root: {
        top: "auto",
        bottom: 0,
        display: "flex",
        alignContent: "end"
    },
}));

export default useStyles;
