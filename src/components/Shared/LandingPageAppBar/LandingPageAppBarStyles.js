import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
    appBar: {
        position: "static",
        width: "100%",
        flexGrow: 1,
    },
}));

export default useStyles;

