import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
    appBar: {
        position: "static",
        width: "100%",
        margin: "25px",
        flexGrow: 1,
    },
}));

export default useStyles;

